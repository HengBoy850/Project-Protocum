DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=staff-management
DB_PORT=3307    

PORT=5000
JWT_SECRET=change-this-to-a-long-random-string
POS_URL=http://localhost:5173



ADMIN_SIGNUP_CODE="tiiHeng49756#@$"

# This is the "company access code" new admin/manager accounts must enter on the
# Register page before their request even reaches the pending queue. Treat it like
# a Wi-Fi password — share it verbally/internally with people you're expecting to
# join, not published anywhere public. Rotate it if it ever leaks.
